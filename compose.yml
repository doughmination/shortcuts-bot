services:
  bot:
    build: .
    container_name: clove-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./bot/src:/app/src
      - ./logs:/app/logs
      - ./command-ids.json:/app/command-ids.json